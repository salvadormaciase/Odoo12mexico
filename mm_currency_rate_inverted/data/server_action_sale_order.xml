<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="mm_current_rate_sale_order" model="base.automation">
        <field name="name">current rate sale order mm</field>
        <field name="active">1</field>
        <field name="state">code</field>
        <field name="model_id" ref="model_sale_order"/>
        <field name="trigger">on_create_or_write</field>
<field name="code">
# You can filter the employees which the journals will be created by default
#algo mas
#raise Warning(record.mm_currency_rate)
if record and record.mm_currency_rate_so > 0:
  val = record.mm_currency_rate_so
  record.write({'manual_currency_exchange_rate': 1/val})
</field>
    </record>
</odoo>
